{
  "name": "@chess/pipeline",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsc && node dist/orchestrator.js",
    "pipeline": "tsc && cd .. && node pipeline/dist/orchestrator.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
