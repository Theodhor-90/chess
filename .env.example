# ---- Required in production ----

# Secret key for signing session cookies. Must be at least 32 characters.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# The full origin URL for CORS (e.g., https://chess-platform.fly.dev).
# In development, leave unset for permissive CORS (all origins allowed).
CORS_ORIGIN=

# ---- Optional (have defaults) ----

# "production" or "development". Controls secure cookies, static file serving, etc.
# Default: unset (treated as development)
NODE_ENV=development

# Server port. Default: 3000
PORT=3000

# Server bind address. Default: 0.0.0.0
HOST=0.0.0.0

# Path to SQLite database file. Default: ./data/chess.db
DATABASE_URL=./data/chess.db

# Override path to the frontend static files directory.
# Default: auto-detected relative to server.ts (../../web/dist)
# Only needed if deploying with a non-standard directory layout.
# STATIC_DIR=
